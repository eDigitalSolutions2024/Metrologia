// Opciones de días de pago (tabla `events` del legacy) — el catálogo real de
// facturas ahora vive en el backend (services/cobranza.js), esto es solo la
// lista de opciones del selector del formulario.
export const DIAS_PAGO_OPCIONES = [15, 30, 60, 0];
